# API URL для фронта (обязательно для прода после деплоя)
# Vite подхватывает только переменные с префиксом VITE_

# Разработка (по умолчанию используется http://localhost:5000/api/ если не задано)
VITE_API_URL=http://localhost:5000/api/

# Продакшен — задайте при сборке или в .env.production перед npm run build
# Пример:
# VITE_API_URL=https://research_api.supporthelper.website/api
